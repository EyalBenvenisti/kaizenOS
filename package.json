{
  "name": "example",
  "private": true,
  "version": "2.1.1",
  "license": "Apache 2.0",
  "scripts": {
    "dev": "gatsby develop -H 0.0.0.0",
    "dev:clean": "gatsby clean && gatsby develop -H 0.0.0.0",
    "build": "gatsby build",
    "build:clean": "yarn clean && gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "deploy": "gatsby build && gh-pages -d public"
  },
  "dependencies": {
    "@mkitio/gatsby-theme-password-protect": "^2.1.2",
    "gatsby": "^2.32.13",
    "gatsby-plugin-hotjar-tracking": "^1.1.0",
    "gatsby-theme-carbon": "^2.1.1",
    "netlify-push-webpack-plugin": "^1.2.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  }
}
